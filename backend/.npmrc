# Reduce memory usage during npm install
# Disable audit and fund to save memory
audit=false
fund=false

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Optimize package lock
package-lock=true

